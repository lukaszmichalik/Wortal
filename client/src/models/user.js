export default class User {
  constructor(name, position, dob, email, username, password) {
    this.name = name;
    this.position = position;
    this.dob = dob;
    this.email = email;
    this.username = username;
    this.password = password;
  }
}
