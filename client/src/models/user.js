export default class User {
  constructor(id, name, position, dob, email, username, password) {
    this.id = id;
    this.name = name;
    this.position = position;
    this.dob = dob;
    this.email = email;
    this.username = username;
    this.password = password;
  }
}
