export default class User {
  constructor(username, position, dob, email, password) {
    this.username = username;
    this.position = position;
    this.dob = dob;
    this.email = email;
    this.password = password;
  }
}
